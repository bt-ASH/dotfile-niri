(defwidget temperature []
    (button :class "temperature" :timeout "10s" :onclick "ghostty -e s-tui"
        {temperature >= 80 ? "󱃂  " + temperature + "°C" : temperature >= 60 ? "󰔏  " + temperature + "°C" : "󱃃  " + temperature + "°C"}
    )
)
